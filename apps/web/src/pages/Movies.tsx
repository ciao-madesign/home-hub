import { SectionStub } from "../components/SectionStub";

export function Movies() {
  return (
    <SectionStub
      title="Film"
      description="Libreria film via Jellyfin: copertine, metadati, Direct Play e transcoding hardware come fallback."
      phase="Fase 5 — Media"
    />
  );
}
