import { SectionStub } from "../components/SectionStub";

export function Photos() {
  return (
    <SectionStub
      title="Foto"
      description="Timeline foto e video personali via Immich, album, ricerca e slideshow."
      phase="Fase 5 — Media (dopo Jellyfin)"
    />
  );
}
